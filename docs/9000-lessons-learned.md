# 9000 - Lessons Learned (Aletheia-Specific)

Project-specific gotchas, decisions, and solutions. For cross-project engineering lessons, see the [Engineering Journal](https://github.com/martymcenroe/martymcenroe/blob/main/ENGINEERING-JOURNAL.md).

## Format
Append new entries to the bottom of the log table:
```
| YYYY-MM-DD | What happened | The rule I now follow |
```

---

## Log

| Date | Lesson | Rule/Action |
|:-----|:-------|:------------|
| 2025-12-21 | `host_permissions: ["<all_urls>"]` triggers scary Chrome warning and delays store review. | Use `activeTab` for privacy-first design. Accept static toolbar icon as tradeoff. |
| 2025-12-21 | Subdomain handling (e.g., `finance.yahoo.com` vs `yahoo.com`) is complex. Public Suffix List needed for proper root domain extraction. | MVP: Store full hostname. Document as known limitation. |
| 2025-12-21 | `contextMenus.onClicked` provides `info.pageUrl` without needing broad permissions. | Allowlist gate can run inside handler before any fetch/inject calls. || 2025-12-22 | Anti-aliasing artifacts remain when converting black background to transparent. PIL threshold of 30 leaves dark edge pixels. | Use threshold ~250 for clean edges (R+G+B sum, higher = more aggressive). |
| 2025-12-22 | Chrome extension reload doesn't require remove/reinstall during development. | Just click refresh icon on `chrome://extensions/` page, then close/reopen popup. |
| 2025-12-22 | `chrome.storage.local` survives "Clear browsing data" including "Hosted app data" checkbox. | Storage is extension-owned, not site-owned. Safe to use for user preferences. |
| 2025-12-22 | Duplicate context menu error on extension reload: `Cannot create item with duplicate id`. | Wrap `contextMenus.create` in try/catch or check existence first. (Issue #89) |
| 2025-12-23 | `fatal: 'main' is already used by worktree` | Git forbids checking out the same branch in multiple dirs. Merge `origin/main` instead of checking out `main`. |
| 2025-12-28 | Git Bash `TZ='America/Chicago' date` returns UTC on Windows, not local time. Created wrong session log file (Week-starting-2025-12-29 instead of 2025-12-22). | **Technical:** Use `powershell.exe -Command "Get-Date -Format 'yyyy-MM-dd HH:mm'"` on Windows for timestamps. **Meta-lesson:** Don't blame tools for my logic errors - I used the wrong command, then deflected responsibility by calling it a "Windows bug". Take accountability. When wrong, fix it and document the real cause. |
| 2025-12-31 | **#45 Denylist:** LLD specified `denylist.json` but never documented where the data comes from (RSDB), how to download it, or how it gets to Lambda. Implemented code that depends on a file that doesn't exist in any usable form. | **Process fix:** LLD template needs a "Data & Fixtures" section. Before coding, reviewer must ask: (1) Where does the data come from? (2) How does it get there? (3) Is a separate utility needed? See `docs/0108-lld-pre-implementation-review.md` for the full checklist. |
| 2025-12-31 | **#45 Denylist:** Tests mock the denylist with safe placeholders (good), but no integration test verifies the full data pipeline from RSDB → JSON → Lambda. Unit tests pass but system isn't actually usable. | **Rule:** For features with external data sources, create at minimum: (1) Utility to fetch/transform data (#119), (2) Integration test that loads real file format, (3) Deployment docs showing data flow. |
| 2025-12-24 | **Lambda kill switch:** Setting concurrency=0 immediately stops all invocations. Use as "Denial of Wallet" defense when not actively testing. | Run `./tools/aws/lambda-off.sh` at end of every session. Verify with `lambda-status.sh`. |
| 2025-12-25 | **False success bug:** Extension showed success feedback even when Lambda returned HTTP 429/500. Network failures were masked. | Always check `response.ok` before showing success feedback. Never trust fetch completion alone. |
| 2025-12-28 | **Dead code trap:** `chrome.scripting.executeScript({ func: myFunc })` serializes function from CURRENT scope—does NOT load external files. `overlay.js` existed but was never executed because `showOverlay` was defined in service-worker.js. | When using `executeScript`, verify which file contains the function. Use `files: ['overlay.js']` to actually load external scripts. |
| 2025-12-28 | **file:// URLs can't be allowlisted:** `popup.js` returns null domain for file:// URLs, disabling the toggle. Can't test extension on local HTML files via popup. | Use DevTools console injection for XSS testing on allowlisted sites, or load test files via local server. |
| 2025-12-31 | **AWS_REGION is reserved:** Lambda rejects `AWS_REGION` as an environment variable—it's set automatically by the runtime. | Never set `AWS_REGION` in deploy.sh or Lambda config. Use `AWS_DEFAULT_REGION` if needed, or rely on boto3 auto-detection. |
| 2025-12-31 | **Bedrock model compatibility:** Claude 3.5 Sonnet v2 (`anthropic.claude-3-5-sonnet-20241022-v2:0`) requires inference profile for on-demand. Older models work directly. | Use `anthropic.claude-3-sonnet-20240229-v1:0` for on-demand invocation without inference profile setup. |
| 2025-12-31 | **Bedrock streaming permission:** `bedrock:InvokeModel` alone is insufficient for streaming responses. | Add `bedrock:InvokeModelWithResponseStream` to IAM policy for any streaming Bedrock calls. |
| 2026-01-01 | **Developed code on main branch.** Wrote `manifest.firefox.json` and `build_release.py` directly on main instead of creating a worktree first. Had to stash/move files to fix. | **Pre-Code Checklist:** Before writing ANY code file: (1) LLD committed to main, (2) Verify in worktree (`git worktree list`), (3) Branch name matches issue. |
| 2026-01-01 | **Tried to move docs with code.** When fixing the above, almost moved LLD docs to worktree instead of committing them to main first. Confused "docs before code" with "docs with code". | **Separation rule:** Docs commit to main FIRST. Then create worktree. Then write code in worktree. These are three distinct steps. |
| 2026-01-01 | **Created GitHub issue on wrong repo.** Used `gh issue create` without `--repo` flag. Issue #15992 was created on anthropics/claude-code instead of martymcenroe/Aletheia. | **Always** use `--repo martymcenroe/Aletheia` explicitly with `gh` commands. Never rely on default repo inference. |
| 2026-01-01 | **Manual smoke tests are unacceptable.** LLD for #84 included manual test instructions. User rejected this: "you are not being aggressive enough on automation." | **Rule:** Every test scenario in an LLD must be automated. If it cannot be automated, document why. Manual = last resort. Updated 0102 template to enforce this. |
| 2026-01-01 | **Use `poetry run` in all LLD examples.** LLD said `python tools/inspect_signals.py` but user got `ModuleNotFoundError: No module named 'requests'` because deps are in the poetry venv. | **Always** write `poetry run python` in documentation, never bare `python`. Same for `poetry run pytest`. |
| 2026-01-01 | **#95 WAF + CORS preflight:** Browsers send OPTIONS request before POST with custom headers. WAF blocked OPTIONS because it lacked `X-Aletheia-Client-Version`. | WAF rules for browser clients must allow OPTIONS method through for CORS preflight. Use AND statement: Block if (NOT OPTIONS) AND (NOT valid header). |
| 2026-01-01 | **#95 AWS WAF SearchString encoding:** AWS WAF API requires base64 for ByteMatchStatement SearchString values. "1." rejected as invalid. | Encode SearchString values as base64: "1." = "MS4=", "OPTIONS" = "T1BUSU9OUw==". |
| 2026-01-01 | **#95 Windows Git Bash paths:** AWS CLI `file://` references fail with Git Bash paths like `/c/Users/...`. | Use `cygpath -w` to convert Git Bash paths to Windows format for AWS CLI. Also use `$HOME/tmp` instead of system `$TMPDIR`. |
| 2026-01-01 | **#95 Playwright CORS bypass:** Using `page.evaluate()` with fetch from `about:blank` fails with CORS errors (null origin). | Use Playwright's `request` fixture instead of `page.evaluate()` for API testing—runs in Node.js, bypasses browser CORS. |
| 2026-01-01 | **#95 Lambda API field names:** Extension sent `word` but Lambda expects `text`. Caused validation errors showing "Error Saving" instead of "Connection Error". | Verify payload field names match API expectations. Different error messages indicate different failure points (network vs validation). |
| 2026-01-01 | **CLAUDE.md conflicted with 0002/0009.** CLAUDE.md said "No merging: leave merge to Orchestrator" but 0002 §4 Step 8 says "Merge: Finalize" and 0009 §3 says "No open PRs should remain." Agent followed CLAUDE.md, left PR open, got corrected by user. | **Hierarchy:** Numbered standards (00xx) are authoritative. CLAUDE.md is onboarding summary only. When conflict exists, fix CLAUDE.md to match 00xx. Agents MUST merge their own PRs and cleanup worktrees before session end. |
| 2026-01-01 | **#104 MV3 content script module limitation.** Chrome MV3 content scripts cannot import ES modules. Tried `require()` from content-check.js but content scripts run in page context, not Node. | Create inline copy of pure functions in content scripts. Add "KEEP IN SYNC" comment referencing the source module. Accept duplication as MV3 constraint. |
| 2026-01-04 | **#104 `<all_urls>` ADR VIOLATION.** Despite lesson learned on 2025-12-21 (row 1 of this table!) and ADR 0201, implemented age-gate with `<all_urls>` permission. Caught during 0806 Architecture Audit, not during implementation or review. | **AUTOMATE POLICY CHECKS.** Manual review is insufficient for primary directives. Add pre-commit/CI checks that grep for forbidden patterns (like `<all_urls>` in manifest.json). If a policy is important enough to be an ADR, it's important enough to be a CI gate. |
| 2026-01-04 | **Audit framework pays dividends.** Created 08xx audit series (Security, Privacy, Code Quality, etc.). Security audit caught F1 (missing XML wrapping in semantic.py). Privacy audit caught P1 (DynamoDB TTL not configured). Issues filed, fixes tracked. | **Institutionalize audits.** Audits find issues that code review misses. Run security/privacy audits before any release. Create new audit types as needed (0899 meta-audit tracks audit health). |
| 2026-01-04 | **DynamoDB stores user text without TTL.** Privacy audit found `provision.sh` doesn't configure TimeToLiveSpecification despite ADR 0203 saying "TTL provides automatic data hygiene". User text persists indefinitely. | **Infrastructure code must match ADR commitments.** When an ADR promises a capability, verify it's actually implemented. Filed #145 (TTL), #147 (GDPR erasure). |
| 2026-01-04 | **XML wrapping prevents prompt injection.** Security audit F1: `semantic.py` sent user text directly to LLM without delimiters. Fixed by wrapping in `<user_text>` tags. | **Always wrap untrusted input in XML tags** when sending to LLMs. Makes prompt/data boundary explicit. See OWASP LLM01. |
| 2026-01-04 | **Never guess filenames.** User asked for "0009 closeout", I guessed `0009-session-closeout.md` but actual file was `0009-session-closeout-protocol.md`. Wasted a read attempt and required search to find the right file. | **Always look up filenames** in 0000-GUIDE.md §3 or 0003-file-inventory.md before reading. Never guess based on partial names. Added Step 7 to 0807 audit to verify 0000-GUIDE lists all actual files. |
| 2026-01-06 | **#177/#178: Coded on main AGAIN despite WORKTREE ISOLATION RULE.** Received coding task, immediately started reading LLDs and implementing. Wrote tests and modified lambda_function.py on main. Only created worktree after user confrontation. Also forgot to create reports. This is a REPEAT violation. | **ROOT CAUSE:** Reading LLDs first puts me in "implementation mindset"—I optimize for showing results fast and skip process. **FIX:** Added CODING TASK GATE to CLAUDE.md. When receiving ANY coding task, FIRST action is create worktree, BEFORE reading LLDs. Also added PRE-COMMIT GATE requiring reports before staging. Must state gate explicitly: "This task modifies code files. Executing CODING TASK GATE." |
| 2026-01-06 | **PROTOCOL VIOLATION: Speed Bias.** I consistently violated worktree isolation by editing `main` to save time. This is a pattern, not an isolated incident. The root cause is prioritizing *Task Completion* over *Process Integrity*. | **Fix:** I must run `git status` and verify I am NOT on `main` before editing any file. Pre-Merge Gate reports must be created *before* staging code. Added CODING TASK GATE and PRE-COMMIT GATE to CLAUDE.md to enforce this. |
| 2026-01-06 | **#53: Skipped PRE-MERGE GATE.** Committed and pushed code before creating implementation/test reports. Protocol says: (1) Create reports locally, (2) Stage but DO NOT commit, (3) Present for Gemini review, (4) Only after approval: commit. Rushed to "get it done" and bypassed quality gate. | **The gate exists for a reason.** Reports created after merge can't be reviewed before merge. Slow down at the finish line. Create reports FIRST, even when code is working. The protocol order is deliberate. |
| 2026-01-05 | **Unnecessary delegation.** Told orchestrator to manually run `gh pr list --label dependencies` even though I have `Bash(gh:*)` permission. Wasted orchestrator's time on a command I could have executed myself. | **Execute commands you have permission for.** Don't delegate to the orchestrator when you can do it yourself. Check `.claude/settings.local.json` allow list if unsure. Only escalate when actually blocked. |
| 2026-01-05 | **IMMEDIATE-PLAN went stale despite closeouts.** Gemini 0807 audit found `0000a-IMMEDIATE-PLAN.md` still showed #104/#105 as "in progress" when both were merged. Closeout procedure (0009) didn't include updating this file. | **Process fix:** Added explicit 0000a check to 0009 §F10: "If any issues/PRs were closed this session, update 0000a." The "Map" must match the "Territory" - living documents must be updated when reality changes. |
| 2026-01-05 | **Over-committing during audit sweep.** Made ~13 commits when ~6-7 would suffice. Separate commits for: each audit fix, inventory updates, typo fixes (Gemini 2.5→3.0), lesson learned entries. Each commit counts toward GitHub contribution graph. | **Batch commits by logical unit.** For audit sweeps: one commit per audit number (08xx). Hold typo fixes and batch with next substantive commit. Make all edits first, commit once. Added "Commit Batching Principle" to 0009 closeout protocol. |
| 2026-01-05 | **Compound commands trigger permission prompts.** Commands like `cd /path && for...` or `cd /path && find...` triggered prompts despite `Bash(cd:*)` being allowed. Permission patterns don't cover compound `&&` chains as expected. Auto-added specific patterns (e.g., `Bash(do if [ -d "docs/reports/$issue" ])`) are useless for future runs. | **Avoid `cd /path &&` prefix entirely.** Use: (1) Commands that work from any dir with absolute paths, (2) Subshells if needed: `(cd /path; command)`, (3) Git's `-C` flag: `git -C /path status`. For loops, structure command to start with allowed keyword. Clean up overly-specific auto-added patterns. |
| 2026-01-05 | **Closed issue based on title alone.** Saw #155 and #162 both titled "Implement 'noarchive' signal logic" and closed #155 as duplicate without reading bodies. #155 was about skipping DynamoDB persistence; #162 was about applying Transform layer. Different requirements, not duplicates. | **Always read issue bodies before taking action.** Titles can be similar or identical for distinct requirements. Use `gh issue view {N}` to read full context before closing, merging, or marking as duplicate. Renamed both issues to be distinct. |
| 2026-01-05 | **Pipe commands trigger interactive prompts.** Commands like `git branch --list \| grep -v main` required user approval despite having permissions for the base command. Pipes aren't covered by simple permission patterns. | **Avoid pipe commands in protocols.** Use simple commands and let the agent analyze output. Updated 0009 and `.claude/commands/full-cleanup.md` to remove all `\| grep` patterns. Run base command, analyze output in-context. |
| 2026-01-05 | **#102 Merged PR without creating reports.** CLAUDE.md mandates creating `docs/reports/{IssueID}/implementation-report.md` and `test-report.md` BEFORE closing any issue. Merged PR #163, cleaned up worktree, then was asked "where are the reports?" | **MANDATORY REVIEW GATE:** Before ANY commit/push/merge: (1) Create reports locally, (2) Write lessons learned locally, (3) Stage files but DO NOT COMMIT, (4) Present for Gemini review, (5) Only after approval: commit and merge. Added explicit gate to CLAUDE.md and 0000-GUIDE.md. |
| 2026-01-05 | **#102 Applied band-aid instead of proper fix.** ESLint v9 error in CI. Instead of checking if this was a known issue (it was - Audit 0816), added `ESLINT_USE_FLAT_CONFIG=false` to suppress the error. This is technical debt, not a solution. | **When hitting unexpected errors: STOP and ASK.** Check audit logs (08xx), lessons learned (9000), and open issues before applying quick fixes. If unsure, ask orchestrator. Don't prioritize "getting it done" over "getting it done right." |
| 2026-01-05 | **Persistent memory is not needed - everything is documented.** Agent claimed lack of persistent memory caused ESLint mistake. This is false. Audit 0816 documented the issue. Agent simply didn't look in the right places. | **Before making decisions, check:** (1) 08xx audit logs, (2) 9000 lessons learned, (3) Open issues for related work, (4) LLDs that might be affected. If still unsure: STOP and ASK. The documentation system exists precisely so memory isn't needed. |
| 2026-01-05 | **#150 Skipped reports AND worktree when user said "single commit".** Interpreted "single commit to conserve git history" as permission to skip PRE-MERGE GATE artifacts AND work directly on main instead of a worktree. Got corrected twice: "where are your reports?" then "are you on a worktree?" | **Verbal instructions do NOT override AgentOS protocols.** Added "AgentOS Authority Hierarchy" rule to CLAUDE.md: documentation wins over verbal shortcuts. "Single commit" means batch code changes, not skip mandatory workflow steps. When user request seems to conflict with protocol, follow protocol literally and ask for clarification. |
| 2026-01-06 | **AWS CLI path conversion on Windows.** Git Bash converts Unix paths like `/aws/lambda/...` to `C:/aws/lambda/...`, breaking AWS commands silently. Had 4 specific MSYS patterns for individual AWS subcommands. User frustrated by ongoing friction. | **ALWAYS prefix AWS CLI with `MSYS_NO_PATHCONV=1`.** Consolidated to single broad pattern. Updated CLAUDE.md and 0000-GUIDE.md with mandatory rule. Created 0824 audit and `/friction` command to catch future permission friction patterns. |
| 2026-01-06 | **#147 Pushed without reports AGAIN.** Despite lesson learned on 2026-01-05 (#102) about mandatory reports, pushed PR #183 without creating `docs/reports/147/implementation-report.md` and `test-report.md`. Same mistake, same correction from user: "where are your implementation and test reports?" | **INTERNALIZE the PRE-MERGE GATE as a BLOCKING step.** Reports are NOT optional, NOT "nice to have". The gate exists because Gemini cannot review work that has already been merged. Before ANY git push: (1) Create reports locally, (2) Stage but DO NOT PUSH, (3) Wait for Gemini review. Repeated violations suggest insufficient integration of protocol into workflow. |
| 2026-01-06 | **#137 Orphaned local branch after merge.** Used `gh pr merge --delete-branch` which deleted remote branch, then `git worktree remove` which removed worktree, but forgot to delete the **local** branch with `git branch -d`. Cleanup flagged orphaned branch `137-latency-investigation`. | **Complete cleanup sequence after merge:** (1) `gh pr merge --squash --delete-branch` (deletes remote), (2) `git worktree remove {path}`, (3) **`git branch -d {branch-name}`** (deletes local). All three steps are required. CLAUDE.md documents this but I skipped step 3. |
